# ---------------------------------------------------------
# HOMEWORK 4: FUNCTIONS CALLING FUNCTIONS 🔧
# ---------------------------------------------------------


# TASK 1: Temperature Converter Report
# Write three helper functions:
#   - celsius_to_fahrenheit(c)  → returns (c * 9/5) + 32
#   - celsius_to_kelvin(c)      → returns c + 273.15
#   - is_freezing(c)            → returns True if c <= 0
# Then write a 'temperature_summary(city, celsius)' function that
# calls all three helpers and prints a formatted report like:
#   London (15°C): 59.0°F | 288.15K | Not freezing
# Test it with: London 15, Moscow -10, Phoenix 45
# Write your code below:




# TASK 2: Shopping Bill
# Write these helper functions:
#   - apply_discount(price, percent)  → returns price after discount
#   - add_tax(price, tax_rate=0.1)    → returns price after 10% tax
#   - format_price(price)             → returns "£12.50" (2 decimal places)
# Then write a 'print_item(name, price, discount=0)' function that:
#   - Applies the discount first, then adds tax, then formats the price
#   - Prints:  "Apple      → £1.10"
# Test it with a few items (some with discounts, some without).
# Write your code below:




# TASK 3: Word Analyser
# Write these helper functions:
#   - count_words(text)            → returns number of words
#   - count_vowels(text)           → returns count of a,e,i,o,u
#   - find_longest_word(text)      → returns the longest word
#   - is_palindrome(word)          → returns True if word reads same backwards
# Then write an 'analyse(text)' driver function that calls all four
# helpers and prints a neat report.
# Test it with: "racecar level python programming"
# Write your code below:




# ⭐ BONUS CHALLENGE:
# Build a "number game" using small functions:
#   - is_prime(n)         → True if n has no divisors except 1 and itself
#   - prime_factors(n)    → returns a list of prime factors of n
#   - number_report(n)    → prints whether n is prime, and if not, its prime factors
# Example:
#   number_report(12)  → "12 is not prime. Factors: 2, 2, 3"
#   number_report(7)   → "7 is prime!"
# Write your code below:

